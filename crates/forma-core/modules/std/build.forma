@struct type TextFile = {
    path: String,
    content: String,
};
@enum type Artifact = {
    TextFile,
};
@struct type OutputPlan = {
    files: Array(Artifact),
};
{
    TextFile,
    Artifact,
    OutputPlan,
}
