@struct type TextFile = {
    path: String,
    content: String,
};
@enum type Artifact = {
    TextFile,
};
@struct type OutputPlan = {
    files: Array(Artifact),
};
export {
    TextFile,
    Artifact,
    OutputPlan,
};
