import "core:codec" as codec;
import "core:result" as result;

@struct
type Type = {v: Option(String)};

let decode = fn(value) {
    codec.decode(Type, value)
};

let encode = fn(value) {
    codec.encode(Type, value) |> result.unwrap
};

export { Type, decode, encode };
