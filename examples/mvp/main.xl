import raw_user from "./user.json";

fn Optional(item) {
    Union([
        Atom('None),
        Tuple([Atom('Some), item]),
    ])
}

type User = Struct({
    name: String,
    age: Int,
    nickname: Optional(String),
});

let user: User = raw_user;
validate(User, user)

